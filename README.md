# Graphics.h Setup with WinBGIm (C/C++)

This repository provides a **complete step-by-step guide** to set up and use `graphics.h` in **C/C++ (MinGW-w64 & VS Code)** on Windows.

It includes:

* Required header and library files
* Proper folder placement
* Compile & run commands
* Beginner-friendly explanation

---

## 📦 Required Files

You only need these **3 files**:

* `graphics.h` → Header file
* `winbgim.h` → Header file
* `libbgi.a` → Library file

All other files in the package can be ignored.

---

## 📁 File Placement

Copy the files to the following locations:

| File       | Location                                |
| ---------- | --------------------------------------- |
| graphics.h | `C:\mingw64\x86_64-w64-mingw32\include` |
| winbgim.h  | `C:\mingw64\x86_64-w64-mingw32\include` |
| libbgi.a   | `C:\mingw64\x86_64-w64-mingw32\lib`     |

---
or
| File       | Location                                |
| ---------- | --------------------------------------- |
| graphics.h | `C:\mingw64\include` |
| winbgim.h  | `C:\mingw64\include` |
| libbgi.a   | `C:\mingw64\lib`     |
